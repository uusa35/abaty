<Modal
        transparent={true}
        visible={visible}
        onRequestClose={() => console.log('closing')}>
        <View
          animation={movement}
          onAnimationEnd={() => setVisible(false)}
          duration={3000}
          iterationCount={1}
          easing="ease-out"
          style={[styles.modalContainer, {backgroundColor: color}]}>
          <View style={styles.titleContainer}>
            <Icon
              name={message.icon}
              type="font-awesome"
              size={60}
              color="white"
            />
            <Animatable.Text
              animation="pulse"
              iterationCount={1}
              style={styles.title}
              direction="alternate">
              {message.title}
            </Animatable.Text>
          </View>
          <Divider />
          <Animatable.Text
            animation="pulse"
            iterationCount={1}
            style={styles.content}
            direction="alternate">
            {message.content}
          </Animatable.Text>
        </View>
      </Modal>